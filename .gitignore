# Pebble build artifacts
build/
.pebble-cache/
*.pbw
.lock-waf_*

# JS deps (pebble-clay pulled at build time)
node_modules/
package-lock.json

# Secrets — Discord token for local testing. NEVER commit. (full account access)
.secrets/
*.token
.env
.env.*

# Editor / OS
.DS_Store

# Brainstorm scratch (visual companion)
.superpowers/

# Dev validation screenshots (not shipped)
screenshots/
